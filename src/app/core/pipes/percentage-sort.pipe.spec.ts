import { PercentageSortPipe } from './percentage-sort.pipe';

describe('PercentageSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageSortPipe();
    expect(pipe).toBeTruthy();
  });
});
